{
  "name": "concource",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "appsync:build": "cd users && npm install --dev && npm run build && rm -r node_modules",
    "test": "echo test"
  },
  "dependencies": {
    "users": "file:./users"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
